import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('StoreAddEditPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StoreAddEditPage(title: 'StoreAddEdit')));
    //  final titleFinder = find.text('StoreAddEdit');
    //  expect(titleFinder, findsOneWidget);
  });
}
