import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:storeroom_organizer/app/modules/store_add_edit/store_add_edit_module.dart';

void main() {
  initModule(StoreAddEditModule());
  // StoreAddEditController storeaddedit;
  //
  setUp(() {
    //     storeaddedit = StoreAddEditModule.to.get<StoreAddEditController>();
  });

  group('StoreAddEditController Test', () {
    //   test("First Test", () {
    //     expect(storeaddedit, isInstanceOf<StoreAddEditController>());
    //   });

    //   test("Set Value", () {
    //     expect(storeaddedit.value, equals(0));
    //     storeaddedit.increment();
    //     expect(storeaddedit.value, equals(1));
    //   });
  });
}
